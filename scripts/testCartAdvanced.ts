/**
 * OLD ADVANCED CART TEST SCRIPT - DISABLED
 *
 * This script uses the old JSON-based cart structure (cart.items array)
 * and needs to be updated to work with the new normalized Cart + CartItem structure.
 *
 * Use testNormalizedCart.ts instead for testing the new cart system.
 *
 * TODO: Update this script to use Cart.findOne() with include: [CartItem]
 * instead of Cart.create() with items array.
 */

// DISABLED - Needs update for normalized cart structure
export default {};
