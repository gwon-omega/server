/**
 * OLD CART TEST SCRIPT - DISABLED
 *
 * This script uses the old JSON-based cart structure and needs to be updated
 * to work with the new normalized Cart + CartItem structure.
 *
 * Use testNormalizedCart.ts instead for testing the new cart system.
 */

// DISABLED - Needs update for normalized cart structure
export default {};
